/* eslint-disable no-var */

// eslint-disable-next-line import/no-extraneous-dependencies
import { ipcRenderer as ipcImport, IpcRenderer } from 'electron';

// eslint-disable-next-line no-underscore-dangle,@typescript-eslint/no-use-before-define
var __static:string = __static;

// eslint-disable-next-line @typescript-eslint/no-unused-vars
var ipcRenderer:IpcRenderer = ipcImport;
